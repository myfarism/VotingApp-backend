# Server Configuration
PORT=3000
NODE_ENV=development

# Blockchain Configuration
BLOCKCHAIN_RPC_URL=
CONTRACT_ADDRESS=
CHAIN_ID=31337

# Admin Wallet (Hardhat Account #0)
ADMIN_PRIVATE_KEY=
ADMIN_ADDRESS=

# Wallet Funding Configuration
WALLET_FUNDING_AMOUNT=0.1
MIN_WALLET_BALANCE=0.01
WALLET_REFUND_AMOUNT=0.05

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-for-security
JWT_EXPIRY=24h

# Encryption Key (Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=

# Email Configuration (Optional)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-specific-password
# EMAIL_FROM=Voting System <noreply@voting.com>


EMAIL_USER=
EMAIL_PASS=

# API Configuration
API_PREFIX=/api
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# CORS
CORS_ORIGIN=*
